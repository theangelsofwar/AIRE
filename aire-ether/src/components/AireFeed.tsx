import React, { useState, useEffect } from 'react';
// import AireList from '../aireList';
import Entity from '../entity';

interface Props {
  entity: any;
}

const AireFeed = ({ entity }): Props => {
  const [airFeed, setAirFeed] = useState([]);


}


export default AireFeed;
